name: Quick Submit

on:
  pull_request:
    types: [opened, reopened]
    branches: [main]

jobs:
  run-scenario:
    if: startsWith(github.head_ref, 'quick-submit-')
    # Pinned to fork until RDI-Foundation/agentbeats-leaderboard-template#23 is merged and @v1 retagged.
    uses: madvasik/agentbeats-leaderboard-template/.github/workflows/quick-submit-runner.yml@b807ba31f921479e0bcd0f5b3a13dd1e7cdf2645
    permissions:
      id-token: write
      contents: write
